/** Ring + glyph cycle — matches WelcomeHeroRing / KaleLumen.jsx lumenRingFill (4.6s) */
export const RING_CYCLE_MS = 4600;
/** lumenRingFill 0–20% hold at empty dot — skipped on mount so fill starts immediately */
export const RING_EMPTY_FRACTION = 0.2;
export const RING_EMPTY_MS = 920;
export const RING_FILL_MS = 2116;
export const RING_HOLD_MS = 1012;
export const RING_DRAIN_MS = 552;

/** Brief flash when ring resets to start — then fold replays */
export const GLYPH_FLASH_MS = 80;

